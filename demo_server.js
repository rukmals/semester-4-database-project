//const con = require('./util/database');


/*var mysql = require('mysql');
var con = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "",
  database: "mydb"
});


const Query = function(sql){
   con.query(sql, function (err, result) {
    if (err) throw err;
    console.log(result);
  });
}

const Insert = function(err) {
  if (err) throw err;
  console.log("Connected!");
  var sql = "SELECT * FROM e_event WHERE status = '"+'1' + "'";
  Query(sql);

};
con.connect(Insert);*/

/*const db = require('./model/products');
db.show();*/
